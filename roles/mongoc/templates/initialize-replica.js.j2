rs.initiate({
  _id: "{{ config_rep_set_name }}",
  configsvr: true,
  members: [
{% set count = 0 %}
{% for h in groups['config'] %}
{ _id : {{count}}, host: "{{ h }}:{{ config_port }}" },
{% set count = count + 1 %}
{% endfor %}
  ]
})